[pytest]
# Repo root on sys.path so `import quick_audit_mcp.server` resolves the same way
# the routine launches it (`python -m quick_audit_mcp.server` from the repo root).
pythonpath = .
testpaths =
    quick_audit_mcp/tests
    tests/automated
