# Normalize all text files to LF in the repo; checkout as-is on all platforms.
# This prevents CRLF/LF diff noise when files are touched by Windows tools on WSL.
* text=auto eol=lf

# Explicitly binary — never touch line endings.
*.pdf binary
*.png binary
*.jpg binary
*.jpeg binary
*.ico binary
*.zip binary
*.gz binary
